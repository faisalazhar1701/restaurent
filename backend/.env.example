# Production-safe placeholders. Fill before deploy. Do not commit real secrets.

# PostgreSQL (required). Replace with your Render Postgres or external URL.
# From localhost: use the EXTERNAL database URL from Render (Connect → External).
# Internal URL (host dpg-xxx-a) only works from services running on Render.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DB_NAME

# JWT signing (required). Use a long random string in production.
JWT_SECRET=replace_me

# Admin login (optional; defaults shown)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=replace_me

# Server
PORT=3001
NODE_ENV=production
